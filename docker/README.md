# Docker

Dockerfiles and Compose files will be added in **Phase 11**.

Planned:

- `Dockerfile.backend` — multi-stage Node production image
- `docker-compose.yml` — API + optional local Mongo for development
