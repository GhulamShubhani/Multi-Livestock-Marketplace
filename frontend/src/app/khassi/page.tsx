import { categoryMetadata, makeCategoryPage } from '@/components/catalog/makeCategoryPage';

export const metadata = categoryMetadata(
  'Khassi',
  'Browse khassi (castrated goat) listings from trusted sellers.',
);
export default makeCategoryPage('khassi', 'Khassi', 'Castrated male goats ready for purchase.');
